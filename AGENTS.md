# AGENTS.md

本文件约束在 AtlasRouter 仓库中工作的 LLM/Agent。目标是减少不必要改动、避免把样例当事实、保护订阅与节点信息。

## 项目背景

AtlasRouter 是基于 Surge APP 的代理分流套件仓库，涉及：

- Surge 分流规则与主配置模板。
- Surge 脚本与 `.sgmodule` 模块。
- 部署在 Cloudflare Workers 上的订阅生成服务。
- 通过 Worker 环境变量或 Secrets 注入的节点信息。

`references/` 只用于理解历史想法和样例结构，不是事实来源，不应被生产实现直接依赖。

## 信息优先级

当信息冲突时，按以下顺序判断：

1. 当前用户明确要求。
2. 仓库内已实现代码和测试。
3. 本文件与项目文档。
4. Surge 和 Cloudflare Workers 官方文档。
5. `references/` 中的历史样例。

如果仍不确定，先说明不确定点，再提一个必要问题。

## 工作方式

- 先读相关文件，再动手。
- 修改前说明假设、成功标准和验证方式。
- 多步骤任务写简短计划，并按计划推进。
- 只实现当前请求需要的最小变更。
- 不做顺手重构、不整理无关格式、不删除无关死代码。
- 匹配现有风格；如果风格尚未形成，选择简单、直接、可维护的写法。
- 每个改动行都应能追溯到当前请求。

## 安全边界

- 不提交真实节点、订阅链接、鉴权 Token、账号信息或任何密钥。
- 不把敏感值写进 README、示例配置、测试快照或日志。
- Worker 的敏感配置使用 Secrets；非敏感配置才使用普通环境变量。
- 本地开发变量文件如 `.dev.vars*`、`.env*` 不应入库。
- 发现 `references/` 或样例中存在疑似真实敏感值时，不要复制到新代码；必要时提醒用户清理。

## Surge 约定

- 保持规则顺序可审查：精确域名、专用服务和高优先级规则靠前，通用规则靠后。
- `[Rule]` 必须有最终兜底规则，通常是 `FINAL,<policy>`。
- 新增策略组时，确认被规则引用的策略组确实存在。
- 使用 `policy-path`、`include-other-group`、`policy-regex-filter` 等能力前，先确认官方语义和当前配置风格。
- 模块只做局部覆盖或追加；不要把主配置逻辑藏进模块。

## Cloudflare Workers 约定

- 新 Worker 实现默认使用 ES Module `fetch(request, env, ctx)` 形式，除非现有代码已经选择其他风格。
- 从 `env` 读取环境变量和 Secrets，不硬编码节点或 Token。
- 订阅入口默认只开放必要 HTTP 方法；鉴权失败返回明确的 401 或 403。
- URL 路径、查询参数和输出格式要保持可测试。
- 生成订阅内容时，避免用脆弱字符串拼接处理结构化数据；优先使用明确的数据结构再渲染。

## 验证要求

根据改动类型选择最小但有效的验证：

- 文档改动：检查 Markdown 结构、链接、术语一致性。
- Surge 配置改动：检查规则顺序、策略引用、`FINAL` 兜底和模块覆盖范围。
- Worker 改动：运行可用的单元测试或本地请求测试；至少验证鉴权失败、路径不存在和正常订阅输出。
- 无法运行验证时，在最终回复中说明原因和残余风险。

## Git 与改动范围

- 可以在脏工作区中工作，但不要回滚用户已有改动。
- 不运行破坏性命令，如 `git reset --hard` 或无授权的 checkout 覆盖。
- 不提交、不建分支、不推送，除非用户明确要求。
- 如果发现无关问题，记录在回复里，不直接修复。
